# Phase 4 self-review

## Independent adversarial review

- **Final query-state review — both new hypotheses reproduced.** Job and incident controls had been read directly by Refresh and pagination even though Apply was the visible commit action. This could combine a newly edited lookup with an old selected detail. Applying a new query also left old job rows clickable while the replacement request was pending, so a stale row could start a detail read in the new context. The final model is immutable applied query state: control edits are drafts; only Apply captures them. Refresh and Previous/Next use the captured query. Apply resets page one, clears old rows and incompatible detail, and advances a small job-query generation before reading. Rows and detail requests are bound to that generation. Successful list/detail items must satisfy the applied state/exact-ID predicate, including when worker progress changes a selected job's state.
- **Page-shape hypothesis — partially reproduced with synthetic success.** The client could render more items than the declared page size and could leave page 2 displayed after a result set shrank to zero. It intentionally does not require `total === items.length`: count and item SQL statements may observe different committed worker progress. It now rejects negative/non-integer counts through the existing count check, rejects more than 25 returned items, requires the requested page and configured page size, validates every element, and returns an empty out-of-range page to page one without looping.
- **R1 — reproduced.** Refresh reloaded summary/list/incidents but not the selected detail, and a lookup could leave an unrelated selection visible. Filter context now invalidates selection and aborts its request; Refresh re-reads the selected job; successful detail renders its own server observation timestamp; identity/version/controller guards reject late responses.
- **R2 — reproduced.** The former shared `AbortError` path suppressed an active eight-second timeout and could render FastAPI's array-shaped `422.detail` as `[object Object]`. Requests now carry explicit superseded/timeout reasons. Supersession is quiet; an active timeout is unavailable/stale; pagers disable after a failed read. Validation/not-found/transport failures map to bounded operator text.
- **R3 — reproduced with synthetic malformed responses.** Missing summary fields defaulted to believable zeros and a plausible wrong-detail body could render. Small minimum validators now require all six non-negative counts, timezone-bearing observation timestamps, bounded page shape, supported states, arrays, and exact requested detail identity. Invalid success retains only clearly labelled prior data or shows unavailable.
- **R4 — reproduced.** The previous frontend checks were source assertions, not interactions. The Node DOM/fetch harness now executes `operations.js` and covers refresh, filter/unknown lookup invalidation, delayed selection races, timeout and late-after-abort behavior, `422`, malformed success, wrong identity, text rendering, unsafe execution URLs, and GET-only requests.

## Additional findings fixed

- Incident controls shared the same draft/applied ambiguity as jobs. They now use an applied state snapshot for Refresh and pagination and clear the old incident list when Apply commits a different draft.
- A late superseded detail response that failed the applied predicate could briefly clear a newer selected job. Full generation/version/identity/controller checks now run before either rendering or clearing; an A→B delayed-response regression test exercises the production script.
- Recorded-text screening rejected CR, LF, and NUL but not other terminal control bytes. It now rejects all C0 controls and DEL; a disposable ESC canary proves the value does not enter any browser response.
- Persisted error/reference fields were allowlisted by schema but copied verbatim. A targeted recorded-text screen now suppresses credential-like/header/provider-body/JSON/control-character values while retaining useful bounded evidence; disposable SQLite and PostgreSQL canaries cover payload, lease, quota, adapter-key, authorization, provider-body, attempt, and incident fields across every operations response.
- Manual browser verification falsified the original execution-route assumption: n8n 2.39.8 renders `/execution/283` as its 404 view and uses `/workflow/{workflow-id}/executions/{execution-id}`. Only incidents durably contain both identifiers, so only safe incident records become links. Job-source and attempt execution references remain text; no migration, guessed backfill, or hardcoded execution-to-workflow mapping was introduced.
- Backend and frontend execution-link checks independently require numeric execution references, constrained workflow identifiers, and the exact `http://localhost|127.0.0.1:5678/workflow/{workflow-id}/executions/{execution-id}` route with no credentials, query, or fragment. URL construction now uses screened projection values, preventing credential-like workflow text from leaking through the URL after its displayed field was redacted.
- The final adversarial pass found that URL parsers expose an empty trailing `?` or `#` as a falsy query/fragment even though the delimiter remains in the URL. Backend configuration screening now rejects the delimiters themselves and emits a canonical origin; frontend validation requires the complete canonical URL string. Executable tests cover empty and populated query/fragment forms.
- Timezone-less timestamps and out-of-range pages are rejected; pages beyond a changed result set restart at page one rather than presenting an impossible page.
- Backend editor-link construction now rejects non-root configured paths and invalid ports. The existing lease UTC normalization and durable incident linkage checks remain preserved.

## Read-only and scope review

- Protected `RecoveryJobResponse` remains unchanged. Operations handlers use ordinary SQLAlchemy reads only: no mutation services, `FOR UPDATE`, commits, background tasks, external gateways, or worker calls.
- Counts group jobs directly, preventing attempt/incident multiplication. Lists filter/page in SQL; resolved and genuinely unlinked incidents remain inspectable. Pending work without a Lead remains `Pending CRM creation`; completed is never called recovered.
- Dynamic text uses `textContent`. The trace link only carries correlation as a query parameter; intake still requires explicit trace loading and does not change accepted booking identity.

## Browser evidence and residual limits

An isolated authenticated Chrome 148 session opened retained execution `283` at `/workflow/phase3-crm-write-diagnostic/executions/283` and rendered the intended diagnostic workflow, `ID#283`, and its recorded rate-limit failure instead of the prior 404. A second Operations link is intentionally unavailable: retained attempt `292` is known to n8n as a recovery-workflow execution, but its application attempt/job records lack durable workflow identity. Separate operations reads can observe later worker progress, so every successful dynamic section labels its own observation time and no global atomic snapshot is claimed. Zero open incidents is not n8n/NVIDIA/SMTP health. Draft controls have no separate “changes not applied” badge; their effect is nevertheless unambiguous because only Apply captures them. This remains loopback synthetic-demo access, not production authentication. Phase 1–3 workflows, evidence, and retained executions remain unchanged.
